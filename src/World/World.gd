extends Node


# Called when the node enters the scene tree for the first time.
#func _ready() -> void:
#	var player_node = get_tree().get_root().find_node("Player", true, false)
#	player_node.connect("reset_camera_position",self, "set_camera_postion_back_on_player")


# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta: float) -> void:
#	pass

#func set_camera_postion_back_on_player(player_position: Vector2, player_offset: Vector2) -> void:
#	world_camera.current = true
#	world_camera.position = player_position
#	world_camera.offset = player_offset
#	print("Player camera was reset")
	
